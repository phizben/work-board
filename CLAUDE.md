# Work Board — Claude Guidelines

## Git workflow
- Never commit directly to `main`
- All changes go on a feature branch: `git checkout -b feat/short-description`
- Push the branch and open a PR into `main` when the work is ready
- Commit and push after every meaningful change so the PR stays up to date
